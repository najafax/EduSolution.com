# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is not yet scaffolded. It currently contains only `README.md`
(a single-line placeholder: "# EduSolution.com") and no source code, build
tooling, tests, or dependency manifests.

There are no build, lint, or test commands to document yet, and no
architecture to describe, because no code exists.

## Next steps for future Claude instances

When real project files are added (framework choice, package manifest, source
tree, etc.), update this file to document:

- Build, lint, and test commands (including how to run a single test)
- The high-level architecture and how major components fit together
- Key conventions specific to this codebase

Until then, do not assume a framework, language, or structure that isn't
present in the repository — check the current file tree first.
